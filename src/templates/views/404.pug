extends ../layouts/_default

block locals

    - view.name = '404'
    - view.title = `${view.name} - ${site.name}`
    - view.description = 'Page not found.'
    - view.url = `${site.url}/404.html`

block view

  +main()

    article
      header
        h1=view.name
      p=view.description
